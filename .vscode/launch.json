{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "type": "cppvsdbg",
            "request": "launch",
            // change the path to your built executable
            "program": "${workspaceFolder}/bazel-bin/src/encoding/encoding.exe",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build:debug"
        }
    ]
}
